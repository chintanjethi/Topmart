import { RegistrationForm } from "../../features/auth/components/RegistrationForm.tsx";

export function RegistrationPage() {

    return (
        <>
            <title>{ `Register | TopMart` }</title>

            <RegistrationForm/>
        </>
    );
}